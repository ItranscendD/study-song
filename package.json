{
  "name": "studysong",
  "version": "1.0.0",
  "private": true,
  "description": "StudySong — Turn your notes into songs",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
